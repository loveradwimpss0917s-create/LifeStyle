# Cloudflare Web Analytics のビーコントークン(26章c1)。
# Cloudflareダッシュボード > Analytics & Logs > Web Analytics でサイトを追加すると発行される。
# 未設定の場合、ビーコンは出力されない(計測なしでビルド・表示は正常に動く)。
# 本番はCloudflare Pagesの [Settings > Environment variables] に設定する。
PUBLIC_CF_BEACON_TOKEN=
